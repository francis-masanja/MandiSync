import { FirmwareView } from '@/components/FirmwareView';

export default async function FirmwarePage() {
  // No data needed for now – placeholder for future firmware UI
  return (
    <FirmwareView />
  );
}

