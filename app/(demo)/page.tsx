'use client';
import { InteractiveDemoModal } from '@/components/InteractiveDemoModal';

export default function DemoPage() {
  return (
    <InteractiveDemoModal isOpen={true} onClose={() => {}} />
  );
}
